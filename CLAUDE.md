# Veg-Toon Studio (Hindi सब्ज़ी कार्टून YouTube content studio)

**What it is:** Internal team workspace + playbook for building Hindi vegetable-drama (anthropomorphic sabzi) YouTube channels — competitor teardown, ready-to-shoot AI story scripts (image→I2V), and launch/thumbnail guides. NOT a public product; the site is `noindex`.
**Status:** Active — most recent work **2026-07-22 (NEWEST-10): FB SHORTS 16, 17 & 18 BUILT — the VILLAGE set on the 14–15 locks (family · karma · the first COMEDY).** Bob's ask: use everything decoded from the 2M-view reel (character look, real deep backgrounds etc.) in 3 new scripts on the same page, **village** register, different angles — "some drama, some morality, family issue, one funny" — **7–8 scenes MAX**, dialogue continuity. Built: `launch/fb-short-16.html` **सबसे बड़ा कमरा / The Biggest Room** (FAMILY, ends warm: the son finds 'less room' for his father at his own housewarming, the granddaughter carries her blanket across the rain to the leaking mud house, the umbrella closes at the threshold, the cot comes back; thumb 16.5) · `launch/fb-short-17.html` **रोका हुआ पानी / Stopped Water** (KARMA: the seth turns the shared channel into his field at night, the village answers with a pot line from the well, a warning on the bund is laughed off, the hoarded water swamps his own crop, and the hero opens the drain **with the seth's OWN spade** — पानी सबका है comes back on it; thumb 17.5) · `launch/fb-short-18.html` **पेड़ पर दर्ज़ी / The Tailor in the Tree** (COMEDY — first in the series: the tailor dangles from the peepal, contradictory advice from the same aunty (कूदो मत!…/कूद जाओ!…), Tamatar refuses his new ladder "कीचड़ में ख़राब हो जाएगी", **a chai glass hauled up the kite string = the silent beat AND the thumbnail 18.4**, hay-cart rescue directed by Mirchi, and the ladder slides into the pond mud pin-sharp in the DEEP-FOCUS background of the celebration shot — one frame, two payoffs). **New envelope per Bob: 8 shots / 56s** (was 9/60): 5 story beats → payoff → X.7 hero-to-camera (4s) → X.8 narrator picture-only (6s, the eased pull-back); hard cuts 6/5/5; one silent beat + exactly 2 eased moves each; every 14–15 lock kept verbatim (veg-skin, deep focus, squat proportions, grade coding, no-text). **Portraits REUSED from 14–15** (same repertory five; pages carry portrait prompts only for re-render; 16/17 cast Aloo·Tamatar·Mirchi·Matar, 18 casts Pyaz·Tamatar·Mirchi·Matar). Dialogue register locked to the 14/15-proven set (पापा/चाचा/भाई/बहन/सेठ जी — NO बेटा/बिटिया/बच्ची in Veo-facing text), and each film **echoes its opening line at the payoff** (जगह कम है → जगह तो अब बहुत है; पानी तो सबका है → the spade; the useless advice → the moral). **⚠ TEMPLATE BUG FOUND + FIXED in 16–18 (still present in 14/15, low-stakes):** the i2v ending "…end on a clean, stable frame that the next clip continues from without a cut" was keyed to the scene's OWN join instead of the NEXT scene's — 16–18 key it to the next scene (chain-endings only on 16.7, 17.5, 17.7, 18.1, 18.7). **QA: 15 defects from 2 independent sub-agent passes, all fixed.** Story-breaking: (a) **Pyaz visible-but-undefined in 18.3/18.5** — both scenes demanded "the dangling tailor" in frame while the render-ONLY lock excluded him → added to In-frame + full descriptor in both prompts; (b) **18.2's core joke wasn't contradictory** — both advice lines meant "don't fall" → rewritten so the same aunty orders both कूदो मत and कूद जाओ; (c) **17.5 described the bund already breached** one scene before the hero breaches it → "sagging low but not yet broken"; (d) 17.6 said "his own spade" while the scene title promised THE SAME spade → Aloo now takes it out of Tamatar's hand; (e) anonymous-crowd whole-veg protection missing from 18.2's image prompt + 17.4 both prompts; (f) thumbnail sentence duplicated in 16.5/17.5 i2v; Hindi: तेरे जैसे पड़ोस में → **तुझ जैसी पड़ोसन हो तो** (16.2), ज़िद line rebuilt with its subject (**तेरी ज़िद पापा से भी ज़्यादा है**, 16.4), 17-caption **हिसाब ख़ुद बराबर कर लिया**, plus gloss/cast-card/logline/gate consistency fixes (16's "through the front door" claim, 17's one-gate-vs-two, Matar's nonexistent tea stall in 16's card). **Generator = session scratch `fbgen1618/gen.py` (NOT in-repo; emitted HTML is source of truth)** — gate asserts: old human-hands clause absent; skin/deep-focus/proportion/no-text locks in every image prompt; grade+background keeps in every i2v; exactly 2 eased moves ("one of only two camera moves", never "the only") and every other camera locked-off; dur∈{4,6,8} summing 56; ≤9 Hindi words/line, words ≤ dur×2.6; ≤3 named chars/scene; every speaker staged in the ACTION line; thumb chars staged; every in-clip line verbatim in its own i2v AND the batch block; VO lines OUT of all Veo prompts and IN the VO-track block; flow joins never reframe a locked camera; no Hindi line/mood/Delivery repeated across 16–18 or vs 14/15; arc chips == 8; balanced tags; 26 copy buttons/page. **Browser pass (re-run after the fixes): 78/78 copy buttons byte-exact on the real async-clipboard path, 0 console errors, 0 overflow at 390/768/1280, 24/24 details, all links resolve.** Wired: `fb-teardown-2.html` (title/meta/kicker → "18 scripts", intro NEWEST sentence, new **§11** spec + 3 cards + three-engines A/B note, footer), `fb-scripts.html` callout (+16–18, "all 18"), index nav (`🎬 FB Shorts 1–18`), and short 15's next-nav → 16. **Bob's framing: three engines, one look — post 16–18 against 14–15 and read watch time + shares per engine; if the comedy holds watch-time, it is the cheapest angle to scale.** **Pushed + live: commit `55a0451`.** — earlier **2026-07-21 (NEWEST-9): FB SHORTS 14 & 15 BUILT — the PHOTOREAL WHOLE-VEGETABLE karma set, decoded from a 7th reference video (~2M views).** Bob sent one Drive video (37.5s · 720×1280 · 30fps, watermark **`@DARKSCIENCEAI`** — another creator's) and asked for **2 new scripts on `fb-teardown-2.html`** with **exactly that character look** ("they totally look like vegetables — their hands, body, everything"), that **clearly-visible background** ("the whole image looks like a scene"), his named cast (**aloo, mirchi, tamatar, pyaj, matar aunty**), **karma as the only thing taken from it** and a **different storyline** (explicitly: do not copy their concept). **⚠ BOB'S TWO READS WERE BOTH RIGHT AND BOTH ARE NOW PROMPT LOCKS.** (1) **Exposed skin = the vegetable's own colour/material.** Verified at full res: the tomato's hands are glossy red with specular highlights on each knuckle and **no glove cuff or seam**; the carrot woman's horizontal growth rings **continue uninterrupted down her throat into her collar** and her bare ankles are orange; the cabbage woman has green hands, forearms and toes; the toddler's bare soles match his own head. **There is no human-skin shader anywhere in that film.** Every prompt we shipped up to Short 13 ended the character block with *"the hands are ordinary human hands"* — **that clause is deleted** and replaced with the opposite instruction (hands/fingers/wrists/neck/forearms/ankles/bare feet = same colour+material as the head; "absolutely NO ordinary human skin tone anywhere"). The QA gate now **fails the build** if the old clause survives. (2) **Deep sharp fully-built sets** — measured Laplacian sharpness is **flat across the frame** (background as crisp as the foreground) in 4 of 5 shots → new **DEEP FOCUS + background-depth lock** banning the empty blurred backdrop, in every image AND video prompt. **OTHER MEASURED FINDINGS (all now in `fb-teardown-2.html` §09):** bodies are **squat, 3.0–4.2 heads tall** (only the rich carrot wife is elongated at 5.9) — **not** the "correctly-proportioned adult human body" our old prompts asked for; **nobody has ears or facial hair**; **5 shots joined by 4 HARD CUTS, zero dissolves** — which **contradicts** the no-cut model of Shorts 8–13; **3 of 5 shots have literally ZERO camera movement** (phase-correlation dx=dy=0 across a full 10s) → every shot is now a **locked-off tripod** bar two eased moves per film; rendered at **24fps on a 10-second-capable model** (two shots land on exactly 10.0s) so it is **NOT Veo** and we cannot match its shot lengths (our cap stays 4/6/8); the grade codes **poor = dark+saturated / rich = bright+desaturated** (temperature is warm in *every* shot — the coding is brightness and cleanliness, **not** colour temperature) and the **payoff beats invert the grade of the room they happen in** (mud darkens the bright room, gold lights the dark one) — the strongest single grade idea in the film, and free to copy. **⚠ DELIVERY IS VISUALLY-LED, NOT WALL-TO-WALL:** measured **9 utterances in 37.4s = 14.6 lines/min**, ~9 Hindi words/line, **56.4% of runtime is speech** (strict Silero VAD) with a **5.95s stretch of no words at all** — **less than half** the density of Shorts 8–13. **⚠ THE MORAL IS TWO-PART AND THE SECOND HALF IS A DIFFERENT VOICE:** the hero **turns to camera** and says the first half lip-synced in-clip (addressing the viewer as **दोस्तो**), then a **separate narrator** closes over the villain's punishment shot while no mouth moves — measured **146.5 Hz** vs **219–348 Hz** for every character. Both our films copy this: scene X.9 is picture-only and its lines are **deliberately kept OUT of the Veo prompt** (so Veo cannot lip-sync them) and sit in their own **NARRATOR VO TRACK** block. The reference also has **NO on-screen text and no Follow/Share CTA at all** (ours 1–13 all have one) — flagged as an A/B. **Built:** `launch/fb-short-14.html` **नाली किसने बंद की / The Blocked Drain** (9 shots · 60s · 16 lines · 5 hard cuts · KARMA, ends COLD: Tamatar tips Aloo's 20-year handcart over, a crate jams the storm drain, **he is warned to his face and says "वो मेरा सिरदर्द नहीं"**, the rain backs the water into his own glass shop while the soaked market queues at the cart above the flood line; thumb 14.6) · `launch/fb-short-15.html` **एक बल्ब / One Bulb** (9 shots · 60s · 15 lines · 6 hard cuts · KARMA, ends WARM: Tamatar doubles the generator rate and pulls the wire off Pyaz's board mid-stitch two days before his daughter's exam; Pyaz digs a cracked solar panel out of a scrapyard and relights **the same socket**; when the generator stops the whole lane is black except one shop, and a stool is pushed out across the threshold; thumb 15.6). **One shared repertory cast of five** (Aloo · Tamatar · Pyaz · Matar aunty · Mirchi) — render 5 portraits ONCE, shoot both films. Neutral/Hindu framing kept (their poor family is Muslim-framed; our Aloo wears a **gamcha over one shoulder**, same visual read). **⚠ QA CAUGHT 34 DEFECTS ACROSS 3 SUB-AGENT PASSES, ALL FIXED — several story-breaking:** (a) **the hero caused his own film's flood** — 14.2 originally had Aloo shrug off the jammed crate ("रहने दे, बाद में निकाल लेंगे"), so the karma inverted and the moral was spoken by the man who left the block in → rewritten so **Tamatar is warned and refuses**; (b) **Short 15's payoff bulb was not the bulb we were shown** — 15.4 lit a new bulb on the roof while 15.6 paid off a bulb inside the shop → 15.4 moved inside and now relights **the exact socket** killed in 15.1; (c) **Matar aunty's tea stall was in two places at once**; (d) **three scenes had a speaker who was not in frame** (Mirchi beside the camera in 14.7, both speakers unrenderable in 15.4, and the **hero absent from his own thumbnail frame** in 15.6); (e) **14.5 told Veo that Tamatar "answers" himself** — the generator had no same-speaker branch; (f) **every "flow" join reframed a locked-off camera**, which is impossible and also broke the last-frame-chaining instruction → each join is now either the **SAME locked frame** or a **hard cut**; (g) **the batch i2v copy-block shipped a different final-shot prompt** than the per-scene block (the batch call never passed `last`); (h) 14.3's camera move was physically impossible; (i) Production Note 4 emitted **"Scenes 4.4 / 4.3"** (film and scene numbers transposed); (j) the pages still said **"continuous (no cuts)"** in the kicker/footer while the body said 5 and 6 hard cuts; (k) a badge chip **hard-truncated at 46 chars**; (l) **21 Hindi/word errors** — hard ones were **टोकरी** (basket) while every prompt said *crate* → **पेटी**, **`कहाँ से दूँ`** (देना cannot take "from where") → **लाऊँ**, and **`पूरा बाज़ार बंद हो गया`** which means the market *closed for business*, not that it went dark (same class as Short 12's `चुनाव` bug) → line cut; plus `सामने` as a bare noun subject → **रास्ता** (which also plants the word the moral pays off), habitual `करता हूँ` for a promise → **करूँगा**, and Tamatar apologising in तुम while Pyaz gave him आप → **माफ़ कीजिए**. **Generator = session scratch `fbgen1415/{data,gen}.py` (NOT in-repo; emitted HTML is source of truth)** — QA gate now also asserts: the **old human-hands clause is absent**, the skin/deep-focus/proportion locks are **present in every prompt**, arc chips == scene count, **exactly 2 eased camera moves** and no scene claiming to be "the only" one, **every speaker staged in the ACTION line** (not just listed in frame), **every thumbnail character staged**, and **no flow join reframing a locked-off camera**. Browser pass: **56/56 copy buttons byte-exact on the clipboard path, 0 console errors, 0 page overflow at 390/768/1280, 161 links all resolving, 0 entity leaks.** Wired into `fb-teardown-2.html` (now "7 videos decoded · 15 scripts", new **§09** decode + **§10** cards), `fb-scripts.html`, index nav (`🎬 FB Shorts 1–15`) and short 13's next-nav. **Bob's framing: this is all testing — try every different angle. Post 14–15 against 11–13 and read watch-time + shares.** **Pushed + live: commit `c09d592`.** — earlier **2026-07-20 (NEWEST-8): FB SHORTS 11, 12 & 13 BUILT — the BRIGHT MODERN set (new angles + a rewritten grade).** Bob's ask: 3 more scripts on the same page (`fb-teardown-2.html`, new **§08**), from **different angles — love / life / dhoka / school-time love / family chachi-vs-mummy fight**; **"your image prompts are yellowish types"** → brighter, higher-quality prompts; **different characters** (he named mirchi, muli, strawberry, mango) in **modern looks + modern backgrounds, not village**; and **dialogue that reads continuous like Short 8's**, not discrete. Framing: *"this is all for testing, we are trying every different angle."* **All three asks fixed in the TEMPLATES, not per-scene:** (1) **grade rewrite** — every image prompt now opens with *ultra-detailed 3D, crisp 4K, ray-traced, bright high-key daylight, true neutral white balance, palette of cool white / clear glass / blond wood / mint / coral / teal / sky blue*, then **explicitly bans** *yellow or amber cast, sepia, orange sunset grade, dust haze, film grain, anything rustic or village-styled*; every **video** prompt repeats the grade lock (`do not warm it up, do not add an amber or yellow cast`) so Veo can't drift back between still and clip; (2) **modern world + cast** — glass rooftop café, mint-tiled café counter, the old school reopened as a co-working floor, a product-launch hall, a duplex kitchen island; new cast in present-day city clothes (**Mirchi** teal blazer · **Aam** denim jacket · **Leechi** · **Kiwi** hoodie · **Mooli** lanyard+backpack · **Shimla** charcoal blazer · **Anaar** ivory suit · **Ananas** · **Strawberry** kurti+jeans · **Chukandar** teal co-ord · **Seb** · **Nashpati** · **Angoor**); character model itself UNCHANGED (whole head IS the fruit/veg, its own top = crown, **NO human scalp hair**); (3) **continuity of dialogue** — every i2v prompt now states each line is *a direct answer to the line before it*, *starts the instant the previous finishes*, *never overlaps, no gap of silence*, and the scene plays as **one single continuous conversation rather than separate lines**; plus a **real one-sentence-across-two-mouths handoff in 11.8, 12.6 and 13.8** (the intro note names those exact scenes — an earlier draft claimed handoffs and had none). **Built:** `launch/fb-short-11.html` **आख़िरी बेंच / The Last Bench** (9 shots · 68s · 38 lines · LOVE: school-time love ten years on; Mirchi/Aam, glass partition → glass door as the divider, a paper boat still under the last bench; thumb 11.5) · `launch/fb-short-12.html` **दूसरा नाम / The Second Name** (8 shots · 60s · 33 lines · DHOKA: launch screen carries ONE founder portrait; Mooli never shouts, Ananas has the first-year sketchbook, Anaar the investor asks one question too many; **ENDS COLD — no hug, no reconciliation**, the deliberate A/B against 8–10's held embrace; thumb 12.4) · `launch/fb-short-13.html` **ग्रुप से बाहर / Out of the Group** (10 shots · 72s · 40 lines · FAMILY: Chukandar chachi removes Strawberry bhabhi from the family WhatsApp group + a row of teal boxes down the marble island; Angoor needs one whole-family photo for school; thumb 13.5). **⚠ THREE STORY-BREAKING DEFECTS caught by QA sub-agents, all fixed:** (a) **13.9 could not deliver its own payoff** — the film's engine is Angoor's *पूरे मतलब सब लोग* ("whole means EVERYBODY") but the final shot listed 3 chars with Angoor *behind* the camera → re-staged as a **tablet propped on the marble as a self-timer with all five in the photograph**, and the **≤3-named-chars rule is deliberately broken for this ONE shot** (QA gate has a `five=True` exception allowed only on a final scene) with an on-page production note: load the 3 close-face portraits as Flow Ingredients and let the written locks hold Seb+Nashpati, then check their faces before animating; (b) **Short 12's geography was impossible as one take** — the design studio was "across the street" from the launch hall while the meeting cabin was inside it → studio + cabin moved **one floor up in the same building** with a glass balustrade over the hall; (c) **Short 11's causal chain cancelled itself** — Leechi asked if she'd read his *message* while the logline said no reply was ever sent → now *he came (to the school, not the station) / his phone was off / she deleted the number*. **⚠ PROMPT-LEVEL DEFECTS fixed (bug classes worth sweeping in future sets):** the **continuity lock was generic boilerplate on all 23 mid-film joins** ("the same place, light and moment carrying straight on") — contradicting each scene's own Flow line on joins that cross hours → now a **per-join clause** (`JOINS` dict in the generator), and every time-crossing join states the light changes **DURING** the move (13.4 morning→afternoon, 13.7→late afternoon, 13.8→house lights on); the **hero prop vanished for one clip** (paper boat absent from 11.7); **"a warm wide shot"** survived in both payoff scenes *two sentences after* the anti-yellow lock → "a soft, open wide shot"; **open school books with no text-defusing phrase** (13.4 now "a plain hand-drawn diagram with no writing on the page"), tablet screen defused in 13.9; Delivery strings that said lines were spoken "over each other" while the same prompt said they never overlap → all rewritten to Short 8's construction (*"starting the instant the other stops, though they never once speak over each other"*); 6 dangling arc chips; 12.4's Delivery misordered the lines Veo would be given; Ananas was staged walking into a scene she was excluded from (12.4) and Shimla was in two places at once (12.6). **⚠ HINDI: 21 line fixes** from an independent grammar pass — hard errors were **आप pronoun + तुम imperative** in 13.7 (`आप पहले जोड़ लो` → `आप पहले जोड़ लीजिए`) with Angoor's whole honorific register to her chachi scrambled, **`पहली साल` gender error** in 12's caption (साल is masc. → `पहले साल`), and **`चुनाव` means *election*** in modern spoken Hindi so it broke 12's punchline → **`ग़लती नहीं, फ़ैसला था।`**; plus `टूट गया` (compound), `रहने ही दे रही हूँ` (progressive not habitual), Seb's broken `कौन, ये छोड़ो।`, and `बेटा` removed from Veo-facing text. Word budgets re-balanced after every edit (≤7 words/line, words ≤ dur×2.6, last scene ×2.0). **DECIDED, do not re-litigate:** Chukandar addresses Strawberry with **आप** in 13.9 while Strawberry uses **तुम** — that is devrani→jethani deference and is TRUE to family usage; the उन्होंने/वो reference-honorifics are correct too. **⚠ Short 13 runs on Short 8's beat skeleton** (rupture at 0 → divider across a shared work surface → her side → his side → a young student brokers it → apology at the divider → divider removed on camera); cast/world/register/trigger all differ but the beat map does not — **flagged on its card: do not post 8 and 13 in the same slot.** **Generator = session scratch `fbgen1113/{data,gen}.py` (NOT in-repo; emitted HTML is source of truth)** — QA gate asserts shot=play order, dur∈{4,6,8}, ≤3 named chars (5 only on a flagged final scene), speaker in frame, ≤7 Hindi words/line, word budget, every line verbatim in its own i2v AND the batch block, exactly one thumbnail, **word-boundary** banned-token sweep (the old substring sweep false-positived "hoodie"→"die"), and no Hindi line / mood string / Delivery sentence repeated across 11–13 **or against shorts 8–10**. **Verified across 4 sub-agent passes: 82/82 copy buttons byte-exact on the clipboard path, 0 console errors, 0 page overflow at 390/768/1280, 0 broken links, 111/111 Hindi lines verbatim in both prompt copies, stat tiles recounted, all 13 pages live 200.** Wired into `fb-teardown-2.html` (now "13 scripts", new §08 + spec + 4 cards), `fb-scripts.html`, index nav (`🎬 FB Shorts 1–13`) and short 10's next-nav. **Bob's framing: post 11–13 against 8–10 and read watch-time + shares — two looks, two sets of angles.** — earlier **2026-07-19 (NEWEST-7): FB SHORTS 8, 9 & 10 BUILT — the SPLIT-REEL model, decoded from a 6th reference video.** Bob sent one more Drive video (66.7s, 720×1280, 30fps, ~9 shots: two brothers split the family home over a white line, the rich/paved side vs the poor/mud side) and asked for **3 new scripts on the same page as the 7**, our own storylines but matching its storyline shape, dialogue delivery, scene management and character look. **⚠ THE BIG LESSON — my first read of the reel was WRONG and a sub-agent caught it.** A plain faster-whisper `small` pass dropped whole segments over the music bed, so I read a *30-second silent middle* and wrote all three scripts around a wordless middle. Verification (medium model + temperature-fallback, 3 decodes, **Silero VAD via onnxruntime** + spectrogram + per-second RMS) proved the opposite: **speech = 90.1% of runtime, longest pause anywhere = 1.4s**, and the ‘silence’ was the two young ones talking. **RULE: never characterise a reel's audio from one ASR pass — VAD-verify before writing to it** (whisper hallucinates/loops over loud music; the earlier medium run collapsed into a repetition loop at 41.2s, CR 6.70). **The real model (now documented in `fb-teardown-2.html` §05–07):** (1) opens ON the rupture at second 0 — no set-up, no flash-forward, unlike shorts 6–7's cold-open hook; (2) a physical divider in nearly every frame; (3) **wall-to-wall short dialogue ~35 lines/min**, 4–7 words a line, one breath each, never overlapping, no narrator; (4) **THE ENGINE = the young ones fix it out loud** — they meet at the divider, agree who talks to whom (*तुम अपने पापा को मनाओ, मैं अपने पापा को मनाता हूँ*), then each works on their own parent until he folds; the wives only soften them; (5) apology AT the divider, both talking over each other; (6) **the divider destroyed on camera** + a long held embrace, no moral card. **Built (deliberately different envelopes so they don't read as one template):** `launch/fb-short-8.html` **आधी दुकान / Half the Shop** (9 shots · 68s · 40 lines · Kaddu pumpkin + Karela bitter-gourd split their father's shop with nailed plastic sheeting, scale one side/weights the other; Nimbu + Matar settle it at the sheeting; nails out at dawn; thumb 8.6) · `launch/fb-short-9.html` **दो चूल्हे / Two Stoves** (8 shots · 58s · 33 lines · Tori ridge-gourd chalks the kitchen floor, Pyaaz onion takes the other stove, Amma Adrak ginger has no side to sit on and sits ON the line; two girls split one roti across the chalk; line wiped at first light; thumb 9.2) · `launch/fb-short-10.html` **बीच की दीवार / The Wall in the Middle** (10 shots · 74s · 39 lines · Shimla capsicum lays a HALF-HEIGHT brick wall across Mooli Bapu's courtyard; grandson Chunnu passes the plate over it, umbrella in the rain, sleeps against it holding Dada ji's hand; bricks off by morning; thumb 10.7). **Safety re-stages:** Short 10 was originally a padlocked gate — re-written to a half-height wall with the old man's lane door explicitly still open (never imply anyone kept on one side); every young character is a **"young student"**, never child/kid/boy/girl, and the generator now hard-asserts a banned-token sweep over all Veo-facing text. **Generator + QA gate** = session scratch `fbgen810/{shorts_data,gen_shorts}.py` (NOT in-repo; emitted HTML is source of truth): asserts ≤3 chars/scene, speaker in frame, ≤7 Hindi words/line, words ≤ dur×2.6 (last scene ×2.0 so the held embrace has silence), every line verbatim in its own i2v AND the batch block, exactly one thumbnail scene, banned-token sweep, and **no Hindi line or mood string repeated across the three scripts**. **Three independent sub-agent passes** (browser + 2 content audits) drove ~40 fixes: Hindi grammar (मिटाएँगी fem. plural, रो रही है, हटवाओ, नीची, ज़िम्मे), habitual-aspect lines the 24h timeline couldn't carry, hour-crossing joins now state the light changes DURING the move, S10's 10.3 no longer feeds him before the plate arrives, S9's apology no longer asks for water already staged, caption/hashtag fixes (#DoBhai, #BaapBeta), and `.btwrap{overflow-x:auto}` fixed a 390px page overflow on the teardown. Final browser pass: **0 console errors, 86/86 copy buttons byte-exact on BOTH clipboard paths, 0 page overflow, 0 broken links.** Wired into `fb-teardown-2.html` (now “6 videos decoded · 10 scripts”, new §05 beat map + §06 delivery rules + §07 cards), `fb-scripts.html`, index nav (`🎬 FB Shorts 1–10`) and short 7's next-nav. **Bob's framing: post 8–10 against 6–7 and read watch-time + shares — two models, same week.** — earlier **2026-07-19 (NEWEST-6): FB SHORTS 6 & 7 BUILT — modern topics + a COLD-OPEN HOOK (new model element).** Bob's ask: two more FB Shorts "on the same webpage where the 5 are", 8–9 scenes each, **a hook at the start**, and **modern topics** — he named them: *"tamato is cheated online"* and *"aloo kids are addicted of mobile, they are detained from the school"*. Built exactly to the shorts-1–5 model plus the hook. **`launch/fb-short-6.html` = "The QR That Ate Everything" / जो पैसे कभी लौटे ही नहीं** (Tamatar tomato cart vendor saves a year for his daughter Chhoti's exam laptop → Chukandar beetroot dealer sells at half price and takes the whole amount by QR on the spot → parcel holds a brick, number dead → Kheera cucumber neighbour reports it inside the first hour → account frozen, every rupee back, dealer in an emptied stall → she sits her exam; moral **"पहले जाँचो, फिर पैसे भेजो"**). **`launch/fb-short-7.html` = "Held Back a Year" / एक साल रोक दिए गए** (Aloo potato press-wallah irons till midnight; Munna + Guddi scroll till two; Gobhi Madam cauliflower principal holds both back a year → Aloo never raises his voice: pre-dawn beside his cart he counts one day's earnings into their hands → all phones into a tin box at six, **his own first** → both clear the re-test → moral **"स्क्रीन नहीं, सपने देखो"**). **THE NEW ELEMENT — cold-open HOOK:** scene **X.0 = a 4s flash-forward** lifted out of the middle (6.0 = the brick falling out of the box, pay-in at 6.4; 7.0 = the principal sliding the notice across the desk, pay-in at 7.3), played before anything is explained; the film then **rewinds** into X.1 and runs forward, and that rewind is the **ONLY cut** in either film (every other join keeps the continuity lock + last-frame→next-input chaining). Envelope: **9 shots (hook + 8) / 64s / 4 cast / 15–16 lines, 100% spoken in-clip**, 4 reference portraits each (first shorts with 4), ≤3 named chars per scene, one hero CU = thumbnail frame (6.5 / 7.4), Follow-Share CTA, neutral/Hindu framing. Wired into **`fb-teardown-2.html` (now 7 cards + a "what is new in 6 and 7" note)**, the `fb-scripts.html` callout, index nav (`🎬 FB Shorts 1–7`), and short 5's next-nav. **⚠ Young characters:** the body clause was widened to "grown-ups adult-sized and the young students simply shorter" and they are always "young student", never child/kid (Veo child-safety); "fraud/scam/police/arrest/thief/steal" are also banned from Veo-facing prompts — the karma beat is staged as *account frozen + emptied stall*, never an arrest. Generator = session scratch `fbgen67/{data,gen}.py` (NOT in-repo; emitted HTML is source of truth) — QA gate asserts 9 scenes, X.0=hook/X.1=rewind, shot=play order, dur∈{4,6,8}, ≤6 Hindi words/line, words ≤ dur×2.6, ≤3 chars/scene, speaker in frame, every line verbatim in its own i2v AND the batch block, hair/no-text/continuity locks, banned-token sweep, balanced tags, copy-button count. Independent sub-agent Playwright pass: 0 console errors, 27/27 copy buttons flip + clipboard byte-exact, 9/9 details open, all links resolve, 15/15 + 16/16 lines verbatim in both places, 0 banned tokens in 171k chars of prompt text. **Bug it found + FIXED on ALL 7 short pages:** `.shot{overflow:hidden}` clipped the dialogue table at 390px with no way to scroll to the third column — `.sh-b` is now `overflow-x:auto` (+ narrow-screen wrap on the time/who cells). Live 200. **Bob's framing: this pair is a TEST — "lets try this if it works for us."** — earlier **2026-07-18 (NEWEST-5): STORY 12 — ALL DIALOGUE NOW SPOKEN IN-CLIP + DIALOGUE CLIPS = 10s.** Shilpi's defect (same class as the FB-Shorts one): Story 12 split narration vs dialogue nicely, but the image-to-video (Veo) prompts didn't actually SPEAK the dialogue — only 10 "spotlight" lines lip-synced; the other 56 lines were TRACK shots whose VIDEO prompt said *"DIALOGUE: none spoken in-clip … plays from the external voice track … every mouth closed … no generated voices,"* i.e. the i2v prompts read like plain action/narration. **Fix (`stories/story-12.html`):** all **45 dialogue shots** now embed EVERY listed Hindi line **verbatim** in the VIDEO prompt, spoken **in-clip and Veo3 lip-synced, ONE speaker at a time in order, only that character's veg-mouth moving**; dialogue clips bumped **8s→10s** (shead + `VIDEO PROMPT (Veo3, 10s)` label + `Single continuous 10-second` body + `full 10 seconds`); the **13 narration/ambience shots stay 8s** action-only (incl. the 3 THUMB-FRAME staging shots 7.2/11.1/13.3). **Qualifier-aware** (not blind lip-sync): crowd `(भीड़)`→overlapping crowd voices, no single mouth; `(फ़ोन पर)`→on-screen speaker's mouth moves holding phone; `(फ़ोन की आवाज़)`/`(रिकॉर्डेड)`/`रिकॉर्डिंग —`→thin/tinny phone-earpiece or venue-PA playback, NO on-screen mouth; `(फुसफुसाकर)`→hushed subtle-mouth whisper. Chips unified `🎧 VOICE TRACK`/`🎤 VEO3 LIP-SYNC` → **`🎤 IN-CLIP`** in BOTH the beat/script listing and the shot blocks (66 lines ×2 + 1 legend). External voice track now carries **NARRATOR VO only**; intro `newmodel` box, length-math (visual floor now `45×10s + 13×8s = 9:14`; runtime UNCHANGED ~10:04–12:07 since word count is identical — audio still governs), Full-Script legend, and "How to produce it" all rewritten to the all-in-clip model. **⚠ 2 shots flagged to Shilpi:** 8.1 (Pyaz's phone confession) & 12.2 (same line replayed over the venue speakers) carry a ~34-word exposé that runs ~13–14s — won't fit 10s; those two are marked "natural pace across the full clip" and are best SPLIT into two takes (or tightened) — awaiting her call. QA: every dialogue line verbatim in its own `-vid` prompt, zero old-model/8s leftovers in dialogue shots, div/pre/section balanced, headless **Playwright copy-button pass** (clipboard carries the full 10s prompt + both Hindi lines). Patch script = session scratch `s12fix/fix.py` (NOT in-repo; emitted HTML is source of truth). — earlier **2026-07-18 (NEWEST-4): FB SHORTS 1–5 DIALOGUE FIX — ALL lines now spoken IN-CLIP.** Dominic reported the defect: each scene's table listed 2–3 dialogue lines but the image-to-video prompt only carried the ONE lip-synced hero line (the rest were on a separate "VO track" — old hybrid model). **Fix (all 5 pages):** every scene's i2v prompt now embeds EVERY listed line verbatim, spoken one at a time in narrative order ("First X says… Then Y replies…", only the current speaker's mouth moves, lines never overlap, first line <1s); 7 scenes re-sequenced so payoffs end on the hero's moral line (1.8, 2.7, 3.8, 4.6, 4.7, 5.7, 5.8); dialogue tables reordered to speaking order + all rows relabelled 🎤 IN-CLIP; per-scene VO boxes + batch "VO TRACK SCRIPT" blocks REMOVED; batch ALL-i2v blocks rebuilt; intro note + stat tiles updated (13/15/13/13/12 lines, 100% in-clip). All word budgets fit (max 15w in an 8s clip @2.6 w/s — no re-timing needed). Also fixed pre-existing Veo trigger "shoves to the front" → "rushes to the front" (short 2, 5 spots). QA gate: every table line verbatim in scene i2v + batch pre, zero VO remnants, banned tokens swept (caption exempt — चोर in FB captions is fine, they're not Veo-facing), headless Playwright pass on copy buttons + nav (24 buttons/page). Fixer = session scratch `fbfix/fix.py` (NOT in-repo; emitted HTML is source of truth). — earlier **2026-07-17 (NEWEST-3): FUNTOO BABA TV TEARDOWN built + pushed + live-verified** — `launch/funtoo-teardown.html` (HTTP 200, linked from index nav). Full frame-by-frame teardown of rival **Funtoo Baba TV** (channel `UCuN8KlvqOiozVwhYcrVsuAA`) on Dominic's ask ("study this channel in detail"). Pulled ALL 13 videos through the **Gonzo residential proxy** (server IP YT-bot-walled; standalone `/root/.local/bin/yt-dlp.bin` + `--extractor-args youtube:player_client=web,tv,android` beats the "video unavailable" block — the default `android_vr` client fails). Method: Hindi auto-caption→transcript+w/s; **frame-diff shot detection** (ffmpeg `scene` filter returns 0 on their soft-dissolve encodes — use 1fps grayscale numpy frame-diff instead, `scratchpad/proc.py`); contact sheets→looks; thumbnail reads. 4 analysis sub-agents by view-tier + 1 independent view-count verifier (matched to live-growth drift). **28.6M views / 13 vids / 55 days.** MEASURED FINDINGS: **(1) longer wins hard** — <5min avg 40K (all 3 duds), 5–8min 1.84M, 8–12min 3.85M; length from MORE beats+faster cuts, not held shots (11:43 champ cuts FASTEST 6.3s/shot, densest 2.88 w/s). **(2) NARRATOR-LED** (~50–70% storyteller VO, dialogue only at peaks) — the biggest channel in our niche wins on the model Story-12 deprioritized; flagged OPEN to test (cheaper, dodges Veo dialogue refusals). **(3) art = CUTE Pixar, NOT photoreal** — single-veg head on full human body, **veg's own top = the crown/"hair" (NOT added human scalp hair — consistent w/ NEWEST-2 Abdulla finding)**, human faces/hands, village-warm vs city-cool colour code, double watermark; flagged OPEN vs Story-12 photoreal call. **(4) spine** = poor hero→luxury-prop detonates plot→love-for-money betrayal→held teary CU→karma stated aloud→justice/reunion/Part-2 cliffhanger; heatwave+concrete-business hook (ice truck/AC/cooling dhaba/water park). **(5) thumbnail = staged spoiler tableau** (confirms Playbook 2.0; flops break it). **(6) repertory casting** — same veg designs recast by role. **(7) dud causes** = no cold-open hook, over-narration, off the betrayal engine, cluttered thumbnails, AI-slop seams, runtime mis-tune, mid-video CTA at the peak. Page = numbers table + runtime chart + formula + voice + art + 13-thumb grid + video-by-video + dud list + **10-point "what WE change"** + 4 open Qs (test narrator model? photoreal vs cute? serialise? draft a heatwave script?). Repo holds `launch/funtoo-teardown.html` + `assets/funtoo/` (13 thumbs + 2 hero frames); scratch build (`scratchpad/funtoo/`, videos deleted post-analysis per bulk-media rule) NOT in repo. **AWAITING Dominic/Shilpi on the narrator-model + cute-vs-photoreal decisions.** — earlier **2026-07-17 FB SHORTS all-5, 2026-07-17): ALL 5 NEW FB SHORTS BUILT + SCENE CONTINUITY.** Dominic OK'd Short 1's look, then asked scenes must NOT "look like there is a cut" (keep continuity) + full scripts 2–5. Built via unified QA-gated generator **`fbgen5/gen.py`** (session scratch) → `launch/fb-short-1..5.html` + `fb-teardown-2.html` (all 5 marked built + linked). **Continuity baked in:** stories stay in a few CONNECTED sets, camera FLOWS between beats (walk/pan/push) not cuts; each scene shows a `Flow →` note of how it joins the previous; every i2v prompt carries a **CONTINUITY lock** (continue previous clip as one unbroken take — same set/light/wardrobe/position, no hard cut); post-note chains **last-frame→next-input (Flow Extend/Frames-to-Video)**. Short 1 re-emitted with the continuity layer (story unchanged). The 5: **1 Returned Wallet (honesty), 2 Shared Tiffin (kindness repaid), 3 Cracked Screen (hard-work rise), 4 Worthless Plot (wisdom>greed), 5 Safe Hands (loyalty vindicated)** — each 8sh/60s, complete payoff arc, no-hair whole-veg reference portraits, hybrid one-speaker dialogue, Follow/Share CTA, neutral/Hindu framing, distinct veg casts; all live 200. **NEWEST-2 (pilot): NEW FB SHORTS REFERENCE STYLE decoded + pilot Short 1 built.** Dominic sent a 2nd Drive folder (`1tO3-zfUHQLQ-R996EDrCkjzRK0JhWSup`) of **5 reference videos** (creator "Abdulla Animates") and asked for 5 NEW FB-Shorts scripts "exactly like that look" — original stories, "proper starting and ending point." I pulled all 5 (gdown), read timestamped contact sheets + hi-res close-ups, and ran Hindi ASR (faster-whisper `small`). **KEY finding — their vegetable style is DIFFERENT from ours: the whole head IS one vegetable with NO human scalp hair (the veg's own top = the crown: okra stem-cap, broccoli florets, strawberry leaf-crown, brinjal calyx, bald potato), big Pixar eyes + eyebrows/moustache only, full human body in real clothes, 3D Pixar-photoreal.** Our v3 model wrongly ADDS human hair — this is the fix. **Their formula:** one underdog hero, family-money-morality drama (honesty rewarded / test-of-sons / cheated-then-karma / loyalty-wrongly-accused / rags-to-riches), a **COMPLETE payoff/reveal ending** (NOT a cliffhanger) + a burned-in **Follow/Share CTA**; teary close-up at the peak = thumbnail. ⚠ Sources use **Muslim framing** (अल्लाह/अब्बा/बरकत) → we convert to **neutral/Hindu** (baked into QA). Delivered + pushed + (Pages propagating): **`launch/fb-teardown-2.html`** (all 5 decoded + locked style spec + formula + 5 original story ideas) and **`launch/fb-short-1.html`** = pilot "The Returned Wallet" (honesty rewarded; okra chai-boy returns capsicum tycoon's wallet → partnership → café). Pilot = 8 shots/60s, 3 no-hair reference portraits, hybrid one-speaker dialogue, payoff + Follow CTA. Generator = session scratch **`fbgen4/gen.py`** (QA gate adds: `no human scalp hair` lock in every image prompt + Muslim-framing tokens banned). `fb-scripts.html` has a callout linking both. **The other 4 (all PLOTTED in the teardown): 2 The Shared Tiffin (kindness repaid), 3 The Cracked Screen (hard-work rise), 4 The Worthless Plot (wisdom>greed), 5 Safe Hands (loyalty vindicated) — build on Dominic's OK after he reviews Short 1's look/shape.** Video files deleted post-analysis (bulk-media rule); scratch teardown in `/tmp/.../scratchpad/newvids`. — earlier **2026-07-17 (NEWEST): FB SCRIPT MODEL REBUILT v3 — PILOT = Script 1 only, pushed + live-verified, AWAITING Dominic's approval before rolling out 2–10.** Dominic reported 3 real defects in the 2026-07-15 FB scripts: (1) **no storyline** — each script was a 6-shot *situation*, not a plotted arc; (2) **no character continuity** — the image prompts were actually byte-identical in the cast block, but text-to-image can't hold a look across separate renders AND every scene crammed in all 5 characters, so outfits drifted every image; (3) **dialogue mixing** — each Veo clip packed 3–4 lines from *different* speakers, and Veo can't lip-sync a multi-speaker exchange so it puts lines on the wrong mouths. **The v3 model (decided with Dominic 2026-07-17) fixes all three:** ① a real **8-beat arc** (setup→exclusion→contrast→kindness→expectation→turn→shock→cliffhanger, cuts on an unfinished reveal); ② **canonical reference portraits** (render each char ONCE, approve, reuse as Flow Ingredients ≤3/scene) + each scene names **only its in-frame chars (≤3)**, never all 5; ③ **HYBRID dialogue** = exactly **one lip-synced hero line per clip** (only that mouth moves in the Veo prompt) + every other line on a **separate VO track** laid in the edit (Story-12 dialogue model applied to FB). Pilot `launch/fb-script-1.html` = 8 shots / 60s / 5 portraits / 8 lip-sync + 6 VO lines, live 200. **Generator = session scratchpad `fbgen3/gen.py`** (NOT in-repo; emitted HTML is source of truth) — QA gate asserts: 1 hero line/clip, hero in-scene, ≤3 named chars/scene, exactly 1 hero CU, 0 banned tokens (word-boundary matched), hero line verbatim in its i2v, VO lines NOT leaked into any i2v, single-speaker lock in each i2v. `fb-scripts.html` card for #1 updated to "60s · 8 shots ✦ rebuilt". **NEXT: on Dominic's OK, rebuild scripts 2–10 to this exact model.** ⚠ Scripts 2–10 are STILL the old broken model until then. — earlier **2026-07-15: FACEBOOK SCRIPTS FULLY REBUILT + pushed + live-verified** — on Dominic's ask, split the single 6.9k-line `launch/fb-scripts.html` into an **index hub + `launch/fb-script-1..10.html` (one page per script — click a card, land on that script's own page)**; **every prompt rewritten to PARAGRAPH (prose) form** (image prompt + image-to-video prompt); **all 10 stories diversified** so no two feel the same (the old poor-bride and neglected-parent clusters were the "some are exactly same" complaint); and **ALL dialogue + action rewritten policy-safe** because Veo 3 was refusing every old clip (dowry/threats/"keep your head down"/humiliation = its harassment filter). New envelope: **49 shots / 348s / 555 Hindi words**, lines ≤6 words, clips 4/6/8s, one hero CU each; dialogue kept lip-synced in the Veo prompt but softened (Dominic's choice over silent+VO). Generator = session scratchpad `fbgen2/gen.py` (NOT in-repo) with a banned-token QA gate (0 leaks across 118 Veo-facing prompt blocks) + headless Playwright test of copy buttons + nav. Earlier **2026-07-15: SCRIPT 13 built + pushed + live-verified** (commit `f696df9`) — `stories/story-13.html`, HTTP 200. **"अदरक दादा और डिजिटल ठग"** = first MODERN script written on Shilpi's explicit ask to *"give me a script 13 exactly like Script 9"* (Script 9 = the reel-addiction story; Shilpi called its dialogue length + structure "perfect"). Topic chosen by Shilpi from 3 options = **digital / OTP-lottery scam on a trusting elder** (villain=Kaju cashew con, hero=Chintu the tech-smart carrot grandson, victim=Adrak Dada ginger). **Cloned Script 9's exact envelope: 16 beats / 44 clips / ~7 min, cute full-veg Pixar style (legacy look, NOT photoreal), ≤9-word lines, one keyframe+I2V per shot, shot=play order.** Generator-built + QA-gated (every spoken line rendered in its own I2V, gender locked per char, zero Veo3 trigger words, titles ≤100 UTF-16); 3 Playbook-2.0 thumbnails + all four packs; linked from index. Generator = session scratchpad `s13/gen.py` (NOT in-repo — emitted HTML is source of truth).
Earlier (2026-07-14, all pushed + live-verified): (1) **Story 11 thumbnails rebuilt to Playbook 2.0 staged-scene style + desc/tags/keywords packs added** (commit `1e32b9f`; keywords also added to Story 12). (2) **Story 10 RE-SHOT** (commit `d568b9d`): 18 clips → **66**, no dialogue line over 9 Hindi words. Before that: Story 12 built + pushed (commit `02403e5`). Site LIVE (internal): https://danielarnold6136.github.io/veg-toon-studio/
**⚠ SCRIPT 9 dialogue-length rule (measured, corrects the CLAUDE.md guideline):** Script 9 — the "perfect" model — has line-1 up to 9 words AND **line-2 up to 8 words** (e.g. "मम्मी! खाना जल गया, पूरा धुआँ हो गया!" = 8w), so the real hard rule is **every line ≤9 words, ≤17 words/clip** (NOT the stricter "line-2 ≤7"). Script 13's QA gate uses ≤9/line + ≤17/clip.
**(3) FACEBOOK is now a live second front (2026-07-14, newest):** Dominic/Shilpi created the FB Page and are shooting **9:16 vertical natively for FB** (not repurposed 16:9). FB competitor teardown of 5 rival videos built + pushed + live-verified (commit `0698e6c`) → `launch/fb-teardown.html`.

## 📘 FACEBOOK (9:16 vertical) — the second front
- **⭐⭐ REBUILT 2026-07-15 (CURRENT — this supersedes the 2026-07-14 build below):** `launch/fb-scripts.html` is now an **index HUB** (10 cards, each links to its own page) + **`launch/fb-script-1..10.html`** one page per script. Each script page = badges, logline, hook, cast chips, per-scene dialogue tables, **PARAGRAPH-form image prompt + PARAGRAPH-form image-to-video prompt** (dialogue baked in, lip-synced), batch prompts, FB caption, overlay sheet, prev/next nav. **New envelope: 49 shots / 348s / 555 Hindi words.** Two reasons for the rebuild (Dominic): (1) *the old prompts were sectioned/bulleted, not paragraphs, and the single mega-page was unmanaged*; (2) ***Veo 3 refused every old clip*** — the dialogue/action depicted dowry, threats, "keep your head down", humiliation = its harassment/illegal-activity filter. **FIX = keep the melodrama but carry it through SITUATION (exclusion, being overlooked, a kindness repaid, a reveal) not abuse; dialogue kept lip-synced but softened (Dominic chose soften-words over silent-clip+VO).** The 10 were also **diversified** so no two feel the same (old poor-bride cluster s1/s8/s10 + neglected-parent cluster s6/s7/s9 was the "some are exactly same" complaint). New titles: 1 The Sofa and the Floor (SERIAL) · 2 Two Envelopes (TAG) · 3 The Seven-Rupee Gajra (WARM, the only moral) · 4 After Twenty Years (VILLAIN WINS) · 5 The Tea-Seller's Chair (KARMA) · 6 One Plate of Food (KARMA) · 7 He Called Her the Help (TAG) · 8 The Division (SERIAL) · 9 The Reserved Seat (TAG) · 10 At the Gate (SERIAL). Generator = session scratchpad **`fbgen2/gen.py`** (NOT in-repo; emitted HTML is source of truth) — a banned-token QA gate (english + hindi trigger tokens) + line≤6w + words/clip≤dur×2.6 + dur∈{4,6,8} + every line embedded verbatim in its i2v + exactly 1 hero CU + i2v never genders the speaker; **verified 0 leaks across 118 Veo-facing prompt blocks** and a headless Playwright pass on copy buttons + nav. Framing still **Hindu/neutral**. ⚠ OPEN: whether even the softened lip-synced lines fully clear Veo on the heaviest scenes — if a clip still refuses, render it silent (drop the dialogue sentence) + add that line as VO in the edit (noted on every page).
- **SUPERSEDED (2026-07-14, commit `9a4455f`) — kept for history:** `launch/fb-scripts.html` was a single page — **78 shots, 542s of video, 1,207 Hindi words**, every scene numbered, every keyframe image prompt, every Veo3 image-to-video prompt with its exact duration, every dialogue line with in-clip timing, per-script batch copy-blocks + FB caption + post-production overlay sheet. 196 copy buttons, all functionally tested headless (execCommand fallback proven; 3 bugs found + fixed pre-push). Framing = **Hindu/neutral per Dominic** (NO Muslim framing — he chose this explicitly).
  - Mix: **3× SERIAL_PART1** (s1 dowry/saas-bahu, s8 jaimala betrayal, s10 chrome family) · **3× TAG/COMMENT bait** (s2 bridge dilemma, s6 maa/nokrani, s9 two sons) · **2× KARMA** (s5 interview flip, s7 one plate of food) · **1× VILLAIN_WINS** (s4 fake-son con, 76s = longest) · **1× WARM, the only one with a moral** (s3 onion-seller romance, deliberately 4×8s long takes to copy the competitor's most efficient render shape).
  - **Generator = session scratchpad `fbgen/{SPEC.md,lock.py,qa.py,gen.py,s1..s10.json}` — NOT in-repo, it dies with the session.** The emitted HTML is the source of truth. `lock.py` builds EVERY prompt string in code (cast/style/anti-drift lock guaranteed mechanically, not by hand). `qa.py` is the gate: asserts line ≤6 words, words/clip ≤ dur×2.6, clip dur ∈ {4,6,8}, shot numbers = play order, exactly 1 hero CU, zero banned Veo3 trigger words, zero अम्मी/ख़ुदा/बेगम, **every scripted line appears verbatim in its own I2V prompt** (unspoken-line bug class), and **speaker gender locked across image vs video prompts** (drift bug class — now structurally impossible: the I2V template never genders the speaker).
  - **Rules baked in, all derived from the teardown:** clips are **4/6/8s only** (never 10) · Hindi speaks at **2.6 words/sec** (measured) · **no line over 6 words** · dialogue starts <0.5s and never stops · one hero CU per script = the thumbnail frame · overlays (PART 1 / COMMENT FOR NEXT PART / TAG KARO) are **post-only in Canva/CapCut**, every prompt hard-bans on-screen text · bottom-right corner must be covered by our badge on export.
- **Teardown page:** `launch/fb-teardown.html` — 5 competitor videos measured frame-by-frame (ffmpeg + Hindi ASR + frame reads), then independently re-verified by a QA sub-agent (it corrected 3 of my numbers — **trust the page, not memory**).
- **Source videos:** Dominic's Drive folder `1_gks5RN-Lk1mDrO_94S75iQRB3l5mlju`, pulled with `gdown --folder`. Downloaded copies were session-scratch only (not in repo); 11 key frames ARE in repo at `assets/fb/`.
- **The 5 rivals = only 3 pages:** v1+v5 = **Ai Fairytales** (pixel-identical logo); v2 = **SN Story AI**; v3 = samosa-logo page; **v4's page is UNKNOWN** (shares only a "Like" sticker template with v2 — do NOT claim same page).
- **Measured shape:** all 9:16 · **31–84s, NOT long-form** · dialogue-led (69–98% of runtime is speech) · **zero silence gaps >0.6s**, continuous music bed · first spoken word **<0.5s in 4 of 5** · 2–4 characters · one held emotional face CU at the peak (also the thumbnail frame).
- **⭐ THE BIG FINDING — serialisation + comment bait.** v1 is labelled **"PART 1"** (burned in, 0–4s) and ends at 63s on **"COMMENT FOR NEXT PART"**, story unresolved. They write the SAME long saas–bahu melodrama we write and ship it as ~60s vertical parts. v5 ends on **"TAG KARO APNE BESTY KO"**. This converts our existing 10-min stories into 8–10 FB posts and fixes the cadence/cold-start problem — **but it is UNVERIFIED that it actually wins** (engagement-bait is a Meta demotion category; a genuine cliffhanger is different in kind but adjacent). Don't bet the page on it until Dominic sends view counts.
- **Endings: 4 of 5 have NO MORAL.** v3 (84s, longest) = con artists steal a child, the reward AND the diamonds and **get away with all of it**, victims oblivious. The ending's job is a REACTION, not closure. ⚠ Our FB page bio currently promises *"Har kahani ek moral ke saath khatam hoti hai"* — **that promise is wrong and needs rewriting** (`launch/meta-launch.html`).
- **Art style CONFIRMED:** veg-head-on-human-body in Indian dress, across all 3 pages; the legacy full-veg look appears NOWHERE → our 2026-07-13 switch was right. **CORRECTION:** they add **human hair on top of the veg head + human facial features** (see Mooli in v4: radish skull + green leaves + black human hair + human eyes/mouth/cheeks). Pure "head IS a single vegetable" reads as a mask; the hybrid reads as a character. **Adopt the hybrid.**
- **⚠ CORRECTS OUR OWN SHOT/DIALOGUE SIZING RULE:** v4 = **four ~7.8s takes, zero hard cuts** (raw 8s AI clips used whole, joined by dips to black), each carrying **~19 Hindi words of two-person back-and-forth**. Real envelope = **lines of 3–6 words, 4–6 lines per 8s clip, ~15–20 words/clip** — NOT our "≤9-word lines, max 2 lines, ≤16 words/clip". Hindi measured at **~2.5 words/sec**. The field ships **8s** clips, supporting the 8s side of the open 8-vs-10s question (still needs Shilpi's render test).
- **Cut speed follows emotion, not a template:** jeopardy = 1.7s/shot (v5 bridge); romance = 7.8s takes (v4).
- **Watermarks:** v2 and v3 still have an **AI-generator "sparkle" glyph in the source footage, covered (badly) by their own corner logo**. Nobody ships a visible generator mark. Keep using our animated badge (Meta names visible 3rd-party watermarks as an unoriginal-content trigger = 90-day demonetisation).
- **FB monetization facts (researched 2026-07-14, primary sources):** FB Reels length cap is **GONE** — all FB video is now a Reel, any length/orientation · In-Stream Ads + Reels Play + Performance Bonus all **died 31 Aug 2025** → replaced by **Facebook Content Monetization, which is INVITE-ONLY with NO published follower/watch thresholds** (every "5,000 followers / 60,000 minutes" figure online is the dead program's criteria — ignore it) · India + Hindi both eligible · **no AI rule at all in FB's monetization policy** (AI content not per-se demonetised; no disclosure needed for a stylised cartoon) · **India RPM is unverifiable from any primary source and is very likely pennies — do not build a P&L on it** · documented on-ramp: **Creator Fast Track = 100k+ YouTube followers → $1,000/mo guaranteed for 3 months** on FB.
- **OPEN / needed from Dominic:** ① page URLs + **view counts** for the 5 videos (files are metadata-stripped — "gaining traction" is unquantified; v2 could be a dud) ② is the "COMMENT FOR NEXT PART" page actually winning? ③ ~~Muslim-family framing?~~ **ANSWERED 2026-07-14: NO — Hindu/neutral only** (माँ/भगवान, never अम्मी/ख़ुदा/बेगम). Baked into the QA gate. ④ serialise Story 10/12 for FB, or write short-form-native scripts?

## Live assets
- Repo: **danielarnold6136/veg-toon-studio** (PUBLIC for Pages, but every page is `noindex,nofollow` = internal). Local `origin` remote has an embedded PAT — pushes work as-is; do NOT print/commit it. Collaborators: SilentAurora245 + mary3862jon.
- Live: https://danielarnold6136.github.io/veg-toon-studio/
- Content on disk: `index.html` (teardown/homepage), `stories/story-1..13.html` (**story-13 = 2026-07-15 MODERN digital-scam story, Script-9 clone: 16 beats / 44 clips / 100 copy-blocks / legacy cute full-veg style / 3 thumbnails / 4 packs**) (**story-10 = RE-SHOT 2026-07-14: 18 beats but 66×10s shots, 113 dialogue lines all ≤9 words, one keyframe per shot, 3 silent reaction shots, ~11:00 — legacy cute full-veg art style, NOT the photoreal style**; **story-11 = narrator-led experiment; thumbnails REBUILT 2026-07-14 to Playbook 2.0 staged scenes (denial / the gate / karma) + desc+tags+keywords added — its 26 beats / 79 shots were NOT touched**; **story-12 = first DIALOGUE-LED hybrid + first photoreal veg-head-on-human script**, built 2026-07-13, 16 beats / 58×8s shots / 41:59 VO:dialogue / ~10–12 min / 10 lip-sync lines / 3 staged-scene thumbnails / title+upload pack), `guides/{story,bgm,thumbnail,launch}-*.html`, `launch/` (meta-launch, postmortem-vegetablesworld, thumbnail-previews/review, video-2), `tracker.html`, `watermark.html`, `updates/2026-07-08.html`, `data/tracker/*.json`.
- Story 12 generator (single source of truth for the page): session scratchpad `s12/{cast,beats_a,beats_b,packs,gen}.py` — edit data files, run `python3 gen.py` to re-emit `stories/story-12.html`. Like story-11's generator it is NOT in-repo.
- Story 10 re-shot generator: session scratchpad `s10/{cast,beats,gen,qa}.py` — `gen.py` splices ONLY the scenes section + full-script + batch-prompt blocks into the shipped `stories/story-10.html` (head/CSS/cast/outro/packs preserved byte-for-byte); `qa.py` asserts the rebuild. **Also NOT in-repo — it dies with the session.** The emitted `stories/story-10.html` is the source of truth; edit the HTML directly next time.

## Key facts
- **Format:** ~5–7 min anthropomorphic vegetable drama (poor-vs-rich + love + betrayal/धोखा + karma), AI-generated image→I2V. Each story page = full Hindi script + scene-mapped image prompts + I2V prompts (Hindi dialogue, English direction).
- **Competitor tracker tooling:** `tools/competitor_pull.py` pulls competitor stats via **Invidious** (server IP is YouTube-bot-flagged so yt-dlp 429s; script rotates/validates Invidious instances, won't overwrite on an empty pull) → dated JSON in `data/tracker/` → regenerates `tracker.html`. Run: `python3 tools/competitor_pull.py`.
- Generator (`build.py`, guides.py, workflow scripts) lives in the SESSION SCRATCHPAD, not this repo — the repo holds the emitted HTML. Rebuild logic is not in-tree.

## Decisions already made
- **PRODUCTION IS VEO3 ONLY** — for BOTH images and video (Dominic, emphatic, 2026-07-09). Do NOT propose or write docs around facelessdev, Kling, Seedance, Midjourney, Flux, DALL·E, Ideogram, or Runway, and no "give me an API key for model X" offers. Thumbnails also come out of Veo3 (Dominic renders + sends them).
- **RENDER LOCK** prompt format (baked into all stories, anti-drift): every image prompt re-states the full anthropomorphic design (can't pin a reusable character); every I2V prompt says "keep EXACTLY the veg from the source image, never turn human, animate only the speaker's mouth"; hard-ban subtitles/captions/text/watermark on every render.
- **Veo3 guardrail bypass:** re-skin trigger words (चोर→"angry crowd/बेईमान", blood→"splash of red paint", जानलेवा→"overcome by heat") — never dull the drama; sweep prompts to 0 triggers. **Confirmed blockers (Story 11, 2026-07-11):** the combo "coughs hard and clutches his chest" (cardiac) + "photo of a child" (child-safety) made Veo3 REFUSE the whole clip. Standing re-skins: **"child"/"children"** → the veg descriptor ("his little apple son", "cheerful young voices") since every character is a fruit/veg anyway; **"clutches his chest"/"coughs hard"** → weary non-medical gesture ("sways, leans on his cart to catch his breath"); **"shoves him"** (violence on an elder) → "bars his way / firmly waves him off"; **"feverish"/"fever"** → "weak, pale / restless half-sleep"; **"collapses"** → "sinks to his knees". Keep benign uses ("clutch the tin/photo to his chest"). Sweep EVERY image+video+thumbnail prompt before shipping.
- **SHOT-ORDER = NARRATION-ORDER (Dominic, 2026-07-11):** within each beat, shots MUST be sequenced in the chronological order of the events the NARRATION describes, and shot number MUST equal play order (1.1 = first on screen) so no manual re-shuffling. QA: read narration → list events → map each shot → confirm order + prop continuity (no appear→vanish→reappear). NOTE: Story 11's script text was independently audited = all 26 beats already correctly ordered — so if Dominic sees images "out of order," it's a RENDER not matching its prompt (regenerate that still), not a script bug. To reorder in the emitted HTML: move the `<div class="shot">` blocks AND renumber shead/labels/`s1-k-` pre-ids + copy-targets.
- **Length knob = BEATS, not words:** ~6 min / ~35 clips; each beat gets 1–3 consecutive ~10s shots (Veo3 = one ~10s clip per image). NEVER trim dialogue words to fit — that kills emotion (angered Dominic).
- **Title formula (corrected 2026-07-13):** YouTube hard-caps titles at **100 UTF-16 units** — total title (hook + tail) must fit inside it; 😭/💔 front-loaded, गरीब/धोखा/kismat-twist pattern, pipe `|`, colon dead; short tail ("Sabji Cartoon | Hindi Kahani"). **Thumbnails (superseded rule):** the old "one giant face ~50%" is DEAD — Playbook 2.0 staged betrayal SCENE is the rule (full frozen story-beat, 2–4 characters, poor↔rich two-camp staging, one wealth prop, split warm/cold light, reads at 120px); verified against the July-2026 1M+ breakout thumbs. Hindi overlay still POST-only (Canva), never AI-rendered.
- **BGM:** avoid NCS (Content-ID claims revenue); use YouTube Audio Library + Pixabay (safe), one shared Suno/Mubert sub for unique per-channel audio.
- **Scaling:** 5–6 channels, everything separated per channel (Google acct + AdSense identity + residential/mobile proxy + antidetect browser); never cross-link.
- **⚠ NARRATOR MODEL CORRECTED (2026-07-13, transcript-measured + adversarially verified):** the "winners run ~80/20 narrator-led" claim from the VW post-mortem is WRONG — 7 of 9 sampled mega-hits (1.4M–7.1M views) are **DIALOGUE-dominant (65–94% dialogue by word share)**, including VW itself (~65–88%). Winners stage dialogue as **ONE external voice track (3–4 voices for all roles) laid over held scenes, loose/no lip-sync** — never per-clip lip-sync. Story 11 (77/23 narrator) stays as-shipped; **Story 12+ = dialogue-led**. Story 12 decisions (Shilpi, 2026-07-13): ~40/60 ratio · **HYBRID voices** (Veo3-native lip-sync ONLY on ~10 spotlight peak lines, ≤12–16 Hindi words each, line ends by ~7s; ALL other dialogue + narration on the external track, mouths closed/neutral in those clips) · runtime target 9:30–10:30 (breakout band is 8:28–13:32, median ~9:20; the 4–6-min cohort stalled/died in June).
- **Veo 3.1 clip cap = 8 SECONDS (4/6/8), NOT ~10s** — all Story-12 prompts say "Single continuous 8-second"; old "~8–10s" pacing is retired for new stories. Prefer hard cuts between fresh 8s clips over Flow Extend. Where the voice track overruns a clip, hold/slow its tail frame in the edit (this is exactly what the winners ship).
- **NEW ART STYLE for Story 12+ (Dominic directive via Bob, 2026-07-13):** photoreal vegetable HEAD on a normal HUMAN body in Indian dress — "whose entire head IS a single real X" phrasing, never "mask/fused/wearing"; confirmed the current winning look on the hottest new channels. **Stories 1–11 keep the legacy cute full-veg lock — do NOT re-render or rewrite them.** Aloo = hero (he leads ~80% of current breakouts; we'd never used him as hero before Story 12). Production still VEO3-ONLY; canonical portrait stills per character (prompts in story-12 cast section) get rendered + approved FIRST (validation pass, incl. one test dialogue clip = shot 4.4), then reused as Flow Ingredients refs (≤3 per scene).
- **Story 12 = "Aloo–Gajar: Modern Family"** — runaway-bride wedding drama (jaimala betrayal, chase = THE thumbnail frame; wedding/love-vs-class is the niche's rising micro-trend as of 2026-07-13). Hard rule from Shilpi: **every thumbnail character must actually appear in the script** (Tamatar cast as Aloo's friend for exactly this reason). Content is genuine general-audience adult melodrama — NOT written for kids (kids watching anyway doesn't change the COPPA label; designing for kids while labelling not-for-kids is the actual violation).
- **Zero-impressions diagnosis (2026-07-13, MFK ruled out — Shilpi confirmed it's OFF):** the AI-content policy is monetization-only, NOT a reach throttle (refuted as the cause). Real mechanism = cold-start starvation: (1) generic titles can't win Search, the only surface a no-history channel has; (2) 3–4 uploads in 2 months = too few algorithm tests (winners ship 3–4×/week); (3) zero external seeding (fix: Shorts teasers + Meta linking each long-form in first 24–48h); (4) CHECK IN STUDIO: video language = Hindi, channel country = India (wrong-language micro-test = 0% CTR = dead test); (5) 5–6-min runtime = half the watch-time per view of the breakouts. Upload checklist baked into story-12.html packs section.
- Moat vs YouTube "inauthentic content" policy (in force 15 Jul 2025) = genuine per-story uniqueness; AI is explicitly allowed, mass-produced sameness is what's targeted.

## ⚠ SHOT/DIALOGUE SIZING RULE (Shilpi, 2026-07-14) — applies to EVERY story
Shilpi's complaint on Story 10: *"dialogues are too long to complete in a 10 sec Veo3 clip; script 9's dialogue length is perfect; script 9 gave 44 scenes, script 10 only 18 — increase the scenes and shorten the dialogue length."*

**Root cause (measured, not guessed):** Story 10 had **18 beats × 1 clip = 18 shots carrying 59 dialogue lines** — 3–4 lines crammed into one ~11s clip, lines up to **24 Hindi words** (≈10.4s of speech: cannot fit ANY Veo clip). Story 9 works because its lines are ~6 words and it runs **1 keyframe PER SHOT**, not per beat.

**The envelope to build every story on (Story 9's, now proven):**
- **1 clip = 10s.** Timing `[0-3s]` action → `[3-7s]` line 1 → `[7-10s]` line 2.
- **No dialogue line over 9 Hindi words.** Line 1 ≤9w, line 2 ≤7w, ≤16w of speech per clip. (Hindi ≈2.3 words/sec.)
- **Max 2 lines per clip.** Never 3–4.
- **One keyframe image prompt + one video prompt PER SHOT** — never one shared keyframe per beat.
- **Shot number = play order** (existing project rule, still holds).
- **To shorten a line: SPLIT it, never cut it.** Dominic's no-trim rule stands — Story 10's rebuild preserved all 618 spoken words while dropping the longest line from 24w → 9w. Splitting a sentence may force a Hindi re-inflection (करने → करनी); that is fine, deleting content is not.
- Shot count falls out of the content: Story 10 needed **66** shots (~11:00 runtime, inside the 8:28–13:32 breakout band), not the 44 Shilpi estimated. Don't force a target count.

**⚠ OPEN RISK — clip length 8s vs 10s.** CLAUDE.md's other decision says Veo 3.1 caps at **8s** (4/6/8). Shilpi was shown that and explicitly chose the **10s** shape anyway (Story 9 renders fine for them at 10s). Story 10 is therefore timed with **line 2 at `[7-10s]`** — meaning **if their Veo returns an 8s clip, line 2 is what gets cut.** If Shilpi reports truncated second lines, re-time to `[0-2s]/[2-5s]/[5-7.5s]` and expect ~85 shots. Do not silently "fix" this — ask.

## ⚠ THUMBNAILS + PACKS — MANDATORY ON EVERY NEW SCRIPT (Shilpi, 2026-07-14)
**Shilpi caught Story 11 shipping OLD-STYLE thumbnails.** Its thumb 2 literally said *"ONE hero face DOMINATES ~half the frame in extreme close-up"*, and the other two were two characters on blurred empty space. That style is **DEAD** and was already superseded by `guides/thumbnail-playbook.html` (**Playbook 2.0 — Story-Scene**). I generated the story after the playbook existed and did not apply it. **Do not repeat this: read `guides/thumbnail-playbook.html` before writing ANY thumbnail prompt.**

**Every thumbnail prompt, in every story, from now on:**
- **A full STAGED SCENE, never a portrait.** 3–4 characters caught mid-drama in a real place. Shilpi, verbatim: *"a proper scene not the empty space (blur space in image) and just two characters — this style is not works."*
- **Sympathy on one side, cruelty on the other.** Victim (crying, ragged, reaching) staged against the rich betrayer(s) (laughing / pointing / turning away). That juxtaposition IS the click.
- **The whole story readable in one frozen beat**, with no text at all (Hindi overlay stays a Canva post-step).
- **ONE clear wealth prop** (luxury car, gold, designer bag), split warm/cold light, reads at 120px.
- **Locked character blocks** (SPECIES / SHAPE & SKIN / FACE NOW / DRESS / DO-NOT, naming the wrong veg) — the anti-drift format.
- **Every character in a thumbnail must actually appear in the script** (standing rule from Story 12).
- Sweep thumbnails for Veo3 trigger words too (`shove`→"bars his way", `collapses`→"sinks to his knees").
- **Thumbnail is the master that attracts the viewer** (Shilpi) — do not treat it as an afterthought.

**Every story page must also ship all four upload packs:** `titles` + **`description`** + **`tags`** + **`keywords`**. Story 11 had only titles; Story 12 had no keywords. Both fixed 2026-07-14. Story 10's packs are the format model (Hindi para → English para → moral → subscribe CTA → AI disclaimer → hashtags).

## 🐞 Two bug classes to sweep in EVERY story (both found in Story 10, 2026-07-14)
1. **Script lines that are never spoken.** The dlg column had lines the I2V prompt silently omitted — verified **7 lines in Story 10** existed on the page but were never rendered (Dada-ji's "शाबाश मेरे लाल...", Munna's "पापा, मेरी फ़ीस...?", Shimla's "हम्म... ठीक है...", Baingan's "छोटे से शुरू कर...", Bhindi's "कर्ज़ उतर गया...", +2). **Check stories 1–9 and 11 for the same.** QA: every dlg line must appear verbatim inside its own shot's I2V `<pre>`.
2. **Character gender mismatch between image and video prompts.** Story 10's image prompts described Shimla Mirch as male ("his smug shine", "kneeling beside him") while **all 16** of his I2V lines said *"only her mouth moving"* → guaranteed render drift. **Sweep every story for speaker-gender consistency across img vs i2v.**

## Dead ends — do NOT redo
- Non-Veo3 tools in any new doc (see decision above).
- AI-rendered on-screen text / Devanagari (broken) → all wanted text goes to post overlay.
- Trimming dialogue to hit a runtime.
- yt-dlp full extraction from the server (429 — bot-flagged); use Invidious — BUT as of 2026-07-13 the public Invidious/Piped ecosystem was also fully IP-walled from here for API pulls; direct YouTube HTML/RSS fetches + the stored residential-proxy route worked instead.
- "One giant face" thumbnails (superseded by Playbook 2.0 staged scenes).
- Treating "~80/20 narrator-led" as the winning model (refuted by transcript measurement — see corrected decision above).

## Our channel
- **@veggie-toons-tv** — https://www.youtube.com/@veggie-toons-tv (UCID `UCLJ36JTM79XDiTuB2FRvRXA`; Meta handles `@veggietoons_tv` / `@veggietoonstv` in `launch/meta-launch.html`). Do NOT ask for the channel link again.
- State as of 2026-07-13: ~3–4 videos, ~7 total views, near-zero impressions. "Made for Kids" confirmed OFF (Shilpi). Shilpi to send Studio stats — when they arrive, run the language/country settings check from the diagnosis above.

## Next (Story 10 — the live ask from Shilpi)
1. ~~Re-shoot Story 10 (18 → 66 clips, ≤9-word lines)~~ **DONE + pushed + live-verified 2026-07-14** (`d568b9d`). Legacy cute full-veg art style deliberately KEPT (Story 10 is a stories-1–11 legacy script; the photoreal veg-head style is Story 12+ only).
2. **Shilpi to render a test clip and confirm the 8s-vs-10s question above** — specifically whether the SECOND line of a clip (`[7-10s]`) survives. That single answer decides whether every story needs re-timing.
3. Open: should stories 1–9 + 11 get the same re-shot treatment? Story 10 was the only one flagged, but the two bug classes above (unspoken lines, gender mismatch) are likely present in others — **ask Shilpi before touching them**, they are not to be rewritten unasked.

## Next (Story 12 production — paused while Story 10 is the focus)
1. ~~Push~~ DONE 2026-07-14 (live, 200).
2. VALIDATION PASS before batch-rendering: 7 canonical portraits + test dialogue clip (shot 4.4) — checks seam/mask artifacts, species drift, Hindi lip-sync on a potato mouth, wedding-beat refusals.
3. Record the voice track (4 voices + narrator; doubling map in story-12 cast section), render 58 shots, assemble in shot order.
4. Upload with the packs-section checklist (Hindi language setting, thumbnail A/B, Shorts teasers within 24–48h).

## Closed — do NOT raise again
- **Veo3-only wording sweep of legacy pages: DROPPED (2026-07-13).** Old tool names (facelessdev/Kling/Seedance/Midjourney/Flux/DALL·E) stay as-is in legacy HTML. Never propose this sweep again.
- **`competitor_pull.py` daily/scheduled auto-pull: NO.** Run it ONLY when explicitly asked for a pull. Never schedule it, never offer to.
